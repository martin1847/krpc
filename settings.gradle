pluginManagement {
    repositories {
        maven {
            url "$MAVEN_REPO"
        }
        gradlePluginPortal()
    }
}
//rootProject.name = 'users.dao'

include 'demo-api','demo-java-server','demo-java-client'

include 'rpc-api','rpc-common','rpc-client','rpc-server'
//include 'grpc-server-spring'

