# use this template
# https://gitlab.btrpc.com/it-devops/ci/-/blob/master/helm/java.1.0/values.yaml


extSecret: example-db-secret

deployment:
  corsAllow: course.btrpc.com
  env:
    APP_ENV: TEST
    LOG_LEVEL: warn
    # Placeholder: the real coordinate is supplied per-environment at deploy time
    # (values override / sealed secret), never committed here.
    QUARKUS_DATASOURCE_JDBC_URL: "jdbc:mysql://mysql.example.invalid:3306/example"