# GENERATED BY BTYX RPC GEN - ${.now?iso_local}

story: ${service.description!serviceFile}
base:
	domain: \$API_COM
	# 对应后台应用名
	app: ${app}
	# 对应后台服务名
	service: ${service.name}
#	token: .xxx.x.x.xx
#	useToken: true
cases:
	<#list service.methods?filter(f->! f.hidden)  as m>
- name: ${m.doc!m.name}
  method: ${m.name}
 <#if m.arg??>
  <#if m.arg.rawType.hasChild() >
  param: {
	<#list m.arg.rawType.fields as f>
	  "${f.name}": "${f.type}"${f?has_next?then(',','')}
	 </#list>
  }
 <#else>
  param: "${m.arg}"
 </#if>
  </#if>
  asserts:
	- code == 0


	</#list>

