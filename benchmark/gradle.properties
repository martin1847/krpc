group=tech.krpc.bench
version=1.1.0-bench

# Consumer-view pins: we test the PUBLISHED artifacts on Maven Central, not the
# worktree source (benchmark is the consumer's perspective). BENCH-001 §1.
krpcVersion=1.1.0
extRpcVersion=1.0.3

# Must match the Quarkus LTS line krpc 1.1.0 aligns to (SPEC §13.1): Quarkus
# 3.33.x LTS -> io.grpc 1.79.0. Held identical across all four benchmarks; the
# only variable is the tested dimension (executor x JVM/native).
quarkusVersion=3.33.2
grpcVersion=1.79.0

org.gradle.logging.level=LIFECYCLE
