# Dependencies
/node_modules

# Track the lockfile: CI uses `npm ci` (needs it) and a global ~/.gitignore may
# ignore package-lock.json — re-include it here (repo rules override global).
!/package-lock.json

# Production
/build

# Generated files
.docusaurus
.cache-loader

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
