Class <tech.krpc.common.RpcConstants> resolves a flag in its static initializer: reads the environment directly via <java.lang.System.getProperty(java.lang.String)> in (RpcConstants.java:28); flags must be resolved lazily on the first-use path (a static initializer is baked into a native image at build time)
Class <tech.krpc.context.KrpcOtel> resolves a flag in its static initializer: reads the environment directly via <java.lang.System.getProperty(java.lang.String)> in (KrpcOtel.java:62); flags must be resolved lazily on the first-use path (a static initializer is baked into a native image at build time)
Class <tech.krpc.context.KrpcOtel> resolves a flag in its static initializer: reads the environment directly via <java.lang.System.getenv(java.lang.String)> in (KrpcOtel.java:62); flags must be resolved lazily on the first-use path (a static initializer is baked into a native image at build time)
Class <tech.krpc.http.server.AbstractHttpHandler> resolves a flag in its static initializer: calls flag accessor <tech.krpc.context.KrpcOtel.enabled()> in (AbstractHttpHandler.java:88); flags must be resolved lazily on the first-use path (a static initializer is baked into a native image at build time)
Class <tech.krpc.server.quarkus.RpcServiceExpose> resolves a flag in its static initializer: reads resolved flag field <tech.krpc.common.RpcConstants.CI_BUILD_ID> in (RpcServiceExpose.java:55); flags must be resolved lazily on the first-use path (a static initializer is baked into a native image at build time)
Class <tech.krpc.server.quarkus.RpcServiceExpose> resolves a flag in its static initializer: reads resolved flag field <tech.krpc.common.RpcConstants.CI_BUILD_ID> in (RpcServiceExpose.java:56); flags must be resolved lazily on the first-use path (a static initializer is baked into a native image at build time)
